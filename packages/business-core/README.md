# @org/business-core

Common database schemas (SQLite/Drizzle) and base business logic entities.

## Usage

Contains table definitions for `core.db`, `liveness.db`, migrations, and utilities for working with site identifiers.
